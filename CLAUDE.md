# Notes for Claude working on this project

This repo is part of the same Cowork project as
`~/Downloads/vaultkeeper-complete` (a separate but related app). See
that repo's `CLAUDE.md` — specifically "Project scope and working
conventions" near the top — for standing conventions that apply here
too: cross-repo scope, the `git status` check at the start of a
session, and the path `!`-escaping check before sending shell
commands.

Nothing MediaTracker-specific has been documented here yet — add
standing goals/instructions/conventions for this repo in this file as
they come up, following the same pattern `vaultkeeper-complete/CLAUDE.md`
uses (standing instructions near the top, dated one-off notes below,
split into an archive file if this grows large).
